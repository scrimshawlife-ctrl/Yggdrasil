# Data model — route atom / route frame

## route_atom (input)

At least one of namespace, rune_id, claimed_lane must be present or the frame is NOT_COMPUTABLE.

## yggdrasil.route.v0 (output)

schema, route_class, scan_class, integrity, forecast_eligible=false, can_promote=false, phenomenal=false, brier=null, epistemic, failure, corpus_ref.
