# Agents

SHADOW honesty on every frame. ADVERSARY dual-use before Hub.

Do not spawn a new mind-specialist bot. Yggdrasil classifies routes. The runtime spine stays in Abraxas-v2.0. Noema stays a different system.

Classify live (Spec 001 T0). Train/Hub gated. Local SoT `~/.yggdrasil` is not in git.
