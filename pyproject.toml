[project]
name = "yggdrasil-topology"
version = "0.1.0"
description = "SHADOW topology specialist. Routes, not minds."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Daniel Meyer" }]

[project.optional-dependencies]
dev = ["pytest>=8"]

[project.scripts]
yggdrasil = "yggdrasil.__main__:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
